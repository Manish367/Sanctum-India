# Local development: leave this unset. Vite's dev server proxies /api to http://localhost:4100
# (configured in vite.config.ts), so the app works with no env file.

# Production (Vercel): set this to your deployed backend's API base URL, including the /api path.
# VITE_API_URL=https://sanctum-india-api.onrender.com/api
